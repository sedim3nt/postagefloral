# Rootbound storefront environment variables
# Copy to .env.local and fill in. Never commit real secrets.

# Public site URL (used for canonical URLs, sitemap, Stripe redirects)
NEXT_PUBLIC_SITE_URL=https://rootbound.shop

# Stripe — required for live checkout. Without this, checkout runs in demo mode.
STRIPE_SECRET_KEY=
